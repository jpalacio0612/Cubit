import { makeStyles } from '@material-ui/core'

export const useStyles = makeStyles({
  home: {
    minHeight: '100vh'
  },
  pagination: {
    margin: '20px',
    display: 'flex',
    justifyContent: 'center'
  }
})
